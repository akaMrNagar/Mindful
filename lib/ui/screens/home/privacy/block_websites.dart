import 'package:flutter/material.dart';

class BlockWebsites extends StatelessWidget {
  const BlockWebsites({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
